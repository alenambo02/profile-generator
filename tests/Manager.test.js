const Manager = require("../lib/Manager");

describe("Manager", () => {
    describe(" ", () => {
    it('is created with name, id, and email');

    expect(Manager).toEqual("teamMembers")
    });
})
